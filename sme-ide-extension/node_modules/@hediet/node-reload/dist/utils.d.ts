import { IDisposable } from "./disposable";
export declare class Debouncer implements IDisposable {
    private _timeout;
    run(delayMs: number, cb: () => void): void;
    dispose(): void;
}
type Task<T> = () => Promise<T>;
export declare class AsyncQueue {
    private readonly _queue;
    private _running;
    private _runNext;
    schedule<T>(task: Task<T>): Promise<T>;
    clear(): void;
}
export type Event<T> = (listener: (args: T) => void) => IDisposable;
export declare class EventEmitter<T = void> {
    private readonly _listeners;
    emit(args: T): void;
    event: Event<T>;
}
export declare class Node<T> {
    readonly value: T;
    readonly outNodes: Node<T>[];
    readonly inNodes: Node<T>[];
    constructor(value: T, outNodes?: Node<T>[], inNodes?: Node<T>[]);
}
export declare class Graph<T> {
    readonly roots: Node<T>[];
    static build<T>(roots: T[], getOut: (value: T) => T[]): Graph<T>;
    constructor(roots: Node<T>[]);
}
export {};
//# sourceMappingURL=utils.d.ts.map