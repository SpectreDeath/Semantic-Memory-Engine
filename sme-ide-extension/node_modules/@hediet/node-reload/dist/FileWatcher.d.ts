import { IDisposable } from "./disposable";
export declare class FileWatcher {
    private readonly _handleChanges;
    constructor(_handleChanges: (filenames: string[]) => void);
    private readonly _watchers;
    private readonly _pendingFileWatchers;
    private readonly _changedWatchers;
    addFile(filename: string): IDisposable;
}
//# sourceMappingURL=FileWatcher.d.ts.map