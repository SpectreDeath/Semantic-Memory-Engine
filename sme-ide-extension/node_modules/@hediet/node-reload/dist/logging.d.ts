import { HotReloadOptions } from "./node";
export declare enum LogLevel {
    Off = 0,
    Warn = 1,
    Info = 2,
    Trace = 3,
    Debug = 4
}
export declare function getLogLevel(options: HotReloadOptions['logging']): LogLevel;
export declare class Logger {
    private readonly _logLevel;
    private readonly _root;
    private _indentation;
    private static readonly TREE_BRANCH;
    private static readonly TREE_VERTICAL;
    constructor(_logLevel: LogLevel, _root?: string);
    private _formatPath;
    logHotReloadActive(): boolean;
    logTrackingRequire(request: string, moduleFilename: string, resolvedFilename: string): boolean;
    logSkippingRequire(request: string, filename: string, reason: string): boolean;
    logResolvingError(request: string, filename: string, error: unknown): boolean;
    logFilesChanged(filenames: string[]): boolean;
    logUpdatingModule(filename: string): boolean;
    logClearingModule(filename: string): boolean;
    logModuleUpdated(filename: string): boolean;
    logUpdateFailed(filename: string, consumerCount: number): boolean;
    logEntryModuleUpdateFailed(filename: string): boolean;
    logRecursiveUpdate(stackFilenames: string[]): boolean;
    logPostponeEndUpdate(filename: string, updateCounter: number): boolean;
    private _isLoggingEnabled;
    private _getIndentation;
    private _log;
    indent(): void;
    unindent(): void;
}
//# sourceMappingURL=logging.d.ts.map